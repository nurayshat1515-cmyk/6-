class Zombie:
    def __init__(self, name, hp):
        self.name = name
        self.hp = hp

    def bite(self, damage):
        self.hp -= damage
        return self.hp

z = Zombie("Мясник", 100)

damage = int(input("Введите силу укуса: "))

remaining_hp = z.bite(damage)
print(f"Остаток здоровья: {remaining_hp}")
